context:
  name: sagemaker
  version: "3.13.1"

package:
  name: ${{ name }}
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/${{ name[0] }}/${{ name }}/sagemaker-${{ version }}.tar.gz
  sha256: 76944c27f5baedfcc3d6ef538f6853d1b50b0ce079d0d9dbddb644eeb236ff51

build:
  noarch: python
  script: echo "[sagemaker-feedstock] intentionally not including any files"
  number: 0

requirements:
  host:
    - python ${{ python_min }}.*
  run:
    - python >=${{ python_min }}
    - sagemaker-python-sdk ==${{ version }}

tests:
  - python:
      python_version: ${{ python_min }}.*
      imports:
        - sagemaker
        - sagemaker.train

about:
  homepage: https://github.com/aws/sagemaker-python-sdk/
  license: Apache-2.0
  license_family: APACHE
  license_file: LICENSE
  summary: Open source library for training and deploying models on Amazon SageMaker.
  documentation: https://sagemaker.readthedocs.io/en/stable/
  repository: https://github.com/aws/sagemaker-python-sdk/

extra:
  recipe-maintainers:
    - mollyheamazon
    - jameslamb
    - raybellwaves
